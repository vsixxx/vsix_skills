interface:
  display_name: "User Context"
  short_description: "Save and read Product Design user context"
  default_prompt: "Use $codexpp-product-design-user-context to handle this skill request."
policy:
  allow_implicit_invocation: false

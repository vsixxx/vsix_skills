interface:
  display_name: "Create Data Context"
  short_description: "Create reusable data context and semantic layers"
  default_prompt: "Use $create-data-context to handle this skill request."
policy:
  allow_implicit_invocation: true

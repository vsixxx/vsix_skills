interface:
  display_name: "Prepare For Meeting"
  short_description: "Customer meeting briefs from calendar and notes"
  default_prompt: "Use $prepare-for-meeting to handle this skill request."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Ideate"
  short_description: "Generate visual directions after brief confirmation"
  default_prompt: "Use $ideate to handle this skill request."
policy:
  allow_implicit_invocation: true

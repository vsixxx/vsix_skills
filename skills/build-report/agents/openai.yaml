interface:
  display_name: "Build Report"
  short_description: "Turn reviewed evidence into polished analytical reports"
  default_prompt: "Use $build-report to handle this skill request."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "HubSpot User Guide"
  short_description: "HubSpot CRM reads, drafts, and proposed writes"
  default_prompt: "Use $hubspot to handle this skill request."
policy:
  allow_implicit_invocation: false

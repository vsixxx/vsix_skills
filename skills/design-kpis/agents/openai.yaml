interface:
  display_name: "Design KPIs"
  short_description: "Define KPIs, driver metrics, guardrails, and scorecards"
  default_prompt: "Use $design-kpis to handle this skill request."
policy:
  allow_implicit_invocation: true

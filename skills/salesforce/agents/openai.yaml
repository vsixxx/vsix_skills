interface:
  display_name: "Salesforce User Guide"
  short_description: "Salesforce CRM queries, drafts, and proposed writes"
  default_prompt: "Use $salesforce to handle this skill request."
policy:
  allow_implicit_invocation: false

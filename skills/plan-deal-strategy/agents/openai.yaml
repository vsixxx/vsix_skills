interface:
  display_name: "Plan Deal Strategy"
  short_description: "Post-discovery deal strategy from grounded evidence"
  default_prompt: "Use $plan-deal-strategy to handle this skill request."
policy:
  allow_implicit_invocation: false

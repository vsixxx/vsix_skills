interface:
  display_name: "Follow Up After Call"
  short_description: "Post-call recap, email, CRM note, and internal draft"
  default_prompt: "Use $follow-up-after-call to handle this skill request."
policy:
  allow_implicit_invocation: false
